import { SCREEN_WIDTH, SPEED_LEVEL } from "../../lib/constants";

export const FOOD_SPEED = SPEED_LEVEL[1];
export const SPAWN_X = SCREEN_WIDTH + 50;
export const SPAWN_X_DELTA = 30;
export const SPAWN_Y_PERCENT = 0.9;
